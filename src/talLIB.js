"use strict"
const gravity = 10; //Gravity value in pixels/sec
const tick = 
(function()  {
    let canvasWidth = ctx.canvas.width;
    let canvasHeight = ctx.canvas.height;
   
    //Draws a sinWave
    function drawSin(ctx){
        let scale = 20;

        ctx.save();
        ctx.beginPath()
        ctx.lineWidth = 3;
        ctx.strokeStyle = "red";

        let x = 0, y= 0;
    }
    
})();
